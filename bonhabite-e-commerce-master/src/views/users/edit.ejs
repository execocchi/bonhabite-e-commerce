<!DOCTYPE html>
<html lang="es">

<head>
    <%- include('../partials/head')%>
    <title>Bon Habite</title>
</head>



<body>
    <%- include('../partials/header')%>

    <div class="container">
        <div class="row">



            <section class="row">

                <div class="col-lg-5 col-md-5 col-sm-12 contain_image">
                    <figure class="form-group formfigure">
                        <img class="w-50 py-3" src="/img/usuarios/<%= usuarioDetalle.image%>" alt="avatar">
                    </figure>



                </div>

                <div class="col-lg-7 col-md-7 col-sm-12">

                    <article>
                        <div class="card-body">
                            <h1 class="title_general p-0 m-0" style="font-size: 25px;">Editar el usuario</h1>
                        </div>
                    </article>

                    <div class="signup-form">
                        <form action="/user/edit/<%= usuarioDetalle.id%>?_method=PUT" method="POST"
                            enctype="multipart/form-data">
                            <div class="form-group">
                                <input type="text" class="form-control edit" name="name" placeholder="Nombre"
                                    value="<%= usuarioDetalle.name%>">
                                    <% if (typeof(errors) != 'undefined' && errors.name) { %>
                                        <span class="text-danger"> <%=errors.name.msg %> </span>
                                        <% } %>
                            </div>
                            <div class="form-group">
                                <input type="text" class="form-control edit" name="lastName" placeholder="Apellido"
                                    value="<%= usuarioDetalle.lastName%>">
                                    <% if (typeof(errors) != 'undefined' && errors.lastName) { %>
                                        <span class="text-danger"> <%= errors.lastName.msg %> </span>
                                        <% } %>
                            </div>
                            <div class="form-group">
                                <input type="text" class="form-control edit" name="email" placeholder="Email"
                                    value="<%= usuarioDetalle.email%>">
                                    <% if (typeof(errors) != 'undefined' && errors.email) { %>
                                        <span class="text-danger"> <%=errors.email.msg %> </span>
                                        <% } %>
                            </div>
                            <div class="form-group">
                                <input type="password" class="form-control edit" name="password" placeholder="Clave"
                                    value="<%= usuarioDetalle.password%>">
                                    <% if (typeof(errors) != 'undefined' && errors.password) { %>
                                        <span class="text-danger"><%= errors.password.msg %> </span>
                                        <% } %>
                            </div>

                            <div class="form-group">
                                <input type="password" class="form-control edit" name="confirm_password" placeholder="Repita la clave"
                                    value="<%= usuarioDetalle.password%>">
                                    <% if (typeof(errors) != 'undefined' && errors.confirm_password) { %>
                                        <span class="text-danger"> <%= errors.confirm_password.msg %> </span>
                                        <% } %>
                            </div>

                            <div class="form-group">

                                <!--De esta forma logro conservar y que viaje la imagen anterior de esta forma evito que el usuario seleccione una imagen nuevamente, ( si no lo desea) -->
                                <input type="hidden" name="oldImage" value="<%= usuarioDetalle.image%>">
                                <!-- hidden: esta pero no se ve en el form. -->

                                <label for="exampleFormControlFile1">Eligí una nueva imagen</label>

                                <input type="file" class="form-control-file" id="exampleFormControlFile1" name="image"
                                    placeholder="Seleccione una imagen" value="<%= usuarioDetalle.image%>">
                            </div>

                            <div class="form-group">
                                <button type="submit" class="btn btn btn_comprar">Actualizar</button>
                                <a class="btn btn btn_volver" href="/userAdmin">Volver</a>
                            </div>
                        </form>
                    </div>

            </section>
        </div>
    </div>
    </div>
    <%- include('../partials/footer')%>
    <%- include('../partials/scripts')%>

</body>




</html>